#!/usr/bin/env groovy
pipeline {
      agent any
      stages {
          stage('Build') {
              agent {
                  none { image 'maven' }
              }
              steps {
                 sh "mvn package -Dmaven.test.skip=true"
              }
         }
      }
}