#!/usr/bin/env groovy
pipeline {
      agent none
      stages {
          stage('Build') {
              agent {
                  none { image 'maven' }
              }
              steps {
                 sh "mvn package -Dmaven.test.skip=true"
              }
         }
      }
}