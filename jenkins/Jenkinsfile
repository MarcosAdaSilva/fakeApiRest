pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh '''
                #!/bin/bash
                mvn package -Dmaven.test.skip=true
                '''
            }
        }
    }
}
