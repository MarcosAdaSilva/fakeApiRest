#!/usr/bin/env groovy
pipeline {
      agent none
      stages {
          stage('Build') {
              agent {
                  any { image 'maven' }
              }
              steps {
                 bat "mvn package -Dmaven.test.skip=true"
              }
         }
      }
}